function getLargestExpressionResult (10, 5){
  let result = a + b;
  if (result < (a - b)) {
      return result = a - b;
  }
  if (result < (a * b)) {
      return result = a * b;
  }
  if (result < (a / b)) {
      return result = a / b;
    }
  return result;
}
console.log(result);
